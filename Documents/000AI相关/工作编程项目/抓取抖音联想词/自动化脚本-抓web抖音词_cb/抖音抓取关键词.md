收到！我们就按你同意的这条路线来落地：V0.5｜抖音单平台，platform 写 douyin，一键脚本运行。
我刚尝试把更新（PRD+SOP+一键脚本）直接写进右侧画布，但因为文本末尾有格式冲突没能成功保存。我先把完整更新发在这里，你可以立刻用；如果需要，我再帮你把这些内容写回画布。



⸻

✅ 需求文档（PRD）— V0.5：抖音联想词采集（单平台）

背景 & 目标
	•	将“客户关键词 → 抖音 Web 搜索 → 联想词（文本+截图）→ 结构化表格”全流程自动化，作为 GEO 分析的基础数据。
	•	面向非技术人员：仅通过编辑 CSV 与双击脚本完成全流程。
	•	抓取抖音的web地址为：https://www.douyin.com/

范围（Scope）
	•	平台：仅抖音 Web（platform 字段固定填写 douyin）。
	•	输入：data/input/keywords.csv，字段：client, platform, keyword。
	•	输出：
	•	CSV：data/output/当日/客户_平台.csv
	•	截图：screenshots/当日/客户/平台/关键词_时间戳.png

输出数据字典
	•	date：采集日期（YYYY-MM-DD）
	•	client：客户名
	•	platform：固定 douyin
	•	keyword：输入的关键词
	•	rank：联想词顺序（从 1 开始）
	•	suggestion_text：联想词文本（DOM 优先，OCR 兜底）
	•	page_url：页面 URL
	•	screenshot_path：截图相对路径
	•	source_mode：DOM / OCR / NONE

验收标准（Acceptance Criteria）
	•	对≥2 个客户、每个≥10 个关键词顺利跑完；
	•	每个关键词：
	•	产出至少 1 条联想词文本；
	•	产出至少 1 张截图；
	•	输出字段完整且 UTF-8 编码；
	•	异常关键词重试 ≤2 次，失败记录在日志；
	•	不修改代码，通过 CSV + 脚本即可运行。

非功能性
	•	兼容 Windows / macOS；
	•	--headful 可视模式便于处理验证码；默认无界面更快更稳；
	•	选择器集中到 config.yml，页面小改不需改代码。

质量校验清单（每日 5 分钟抽检）
	•	抽 10 条对比联想词文本与截图是否一致；
	•	单关键词联想词条数异常（为 0 或过少）→ 检查选择器或风控；
	•	source_mode=OCR 占比 > 50% → 检查截图区域/页面结构。

不在范围（Out of Scope）
	•	小红书/其他平台；
	•	自动登录/打码；
	•	并发、代理、浏览器指纹。

后续版本（V1 预留）
	•	多平台适配器（小红书、B站等）；
	•	并发与代理池；
	•	失败率报表与抽样质检脚本。

⸻

🧭 技术执行文档（SOP）— V0.5：抖音联想词采集

1) 前置条件
	•	安装 Python 3 + pip。
	•	首次运行会下载 Playwright 的 Chromium。
	•	（可选）OCR：mac 安装 tesseract / tesseract-lang；Windows 安装 Tesseract 并在 src/ocr.py 注释处填路径。

2) 目录结构（简化）

./
├─ requirements.txt
├─ config/config.yml
├─ data/input/keywords.csv
├─ data/output/           # 运行后生成
├─ screenshots/           # 运行后生成
└─ src/
   ├─ main.py
   ├─ utils.py
   └─ ocr.py

3) 依赖安装（首次/环境变更）

pip install -r requirements.txt
python -m playwright install chromium
# mac 可选：brew install tesseract tesseract-lang
# win 可选：安装 Tesseract 后在 src/ocr.py 指定 tesseract_cmd

4) 输入准备（CSV 模板）

platform 固定写 douyin

client,platform,keyword
思迈特,douyin,体检套餐
思迈特,douyin,糖尿病饮食
永洪,douyin,老人肺部护理

5) 运行方式
	•	可视化调试（推荐首次使用）：

python src/main.py --input data/input/keywords.csv --outdir data/output --shots screenshots --headful

	•	无界面（更快）：

python src/main.py --input data/input/keywords.csv --outdir data/output --shots screenshots

6) 一键脚本（双击即跑）

Windows：run.bat

@echo off
pip install -r requirements.txt
python -m playwright install chromium
python src\main.py --input data\input\keywords.csv --outdir data\output --shots screenshots --headful
pause

macOS / Linux：run.sh

#!/usr/bin/env bash
set -e
pip install -r requirements.txt || true
python -m playwright install chromium || true
python src/main.py --input data/input/keywords.csv --outdir data/output --shots screenshots --headful

运行：

chmod +x run.sh
./run.sh

7) 产出位置
	•	表格：data/output/当日/客户_平台.csv
	•	截图：screenshots/当日/客户/平台/*.png

8) 故障排查
	•	抓不到联想词：config/config.yml 中调整 search_input_selector 或 suggest_* 选择器；
	•	验证码/需登录：加 --headful，手动完成后继续；
	•	OCR 乱码：安装中文语言包或改为元素截图（提高清晰度）；
	•	文件名含特殊字符：代码已做净化，若仍报错，先减少特殊符号。

9) 团队分享
	•	压缩包：打包整个项目给同事 → 解压后按“依赖安装 + 运行方式”执行；
	•	GitHub：推送仓库 → 同事 Clone → 执行脚本；（推荐协作）

⸻

🗓️ 今天就能完成的检查清单
	•	requirements.txt / config.yml / src/*.py / data/input/keywords.csv 放好
	•	pip install ... & python -m playwright install chromium
	•	keywords.csv 写入 2 个客户 × 10 关键词（platform=douyin）
	•	运行 run.bat（Win）或 run.sh（Mac/Linux）
	•	检查 data/output/* 与 screenshots/*
	•	抽样 10 条做“文本 vs 截图”校验

⸻

如果你愿意，我可以把上面的PRD + SOP + 一键脚本内容一并写回右侧画布（含分节标题），并把 src/main.py 的结尾自动修复。需要的话直接说“写回画布”，我就帮你处理。